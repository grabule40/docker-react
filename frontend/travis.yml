sudo: required
services:
    - docker

before_install:
    - docker build -t bubooo-droid/docer-react -f Dockerfile.dev .

script:
    - docker run -e CI=true bubooo-droid/docker-react npm run test -- --coverage